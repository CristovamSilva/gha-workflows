Expressions, Context and Functions

Expression = ${{ <expression> }}
Expression + context = ${{ contextName.VARIABLE }}
Expression + context + function = ${{ toJson(contextName.VAR) }}

Available contexts:
github
env
job
jobs
steps
runner
secrets
strategy
matrix
needs
inputs

Available Functions:
contains
startsWith
endsWith
format
join
toJson
fromJson
hashFiles
  special functions:
    success
    always
    cancelled
    failure
